# Database
DATABASE_URL="postgresql://focus:focus@localhost:5432/focus?schema=public"

# Auth — generate with: npm run hash-password -- "your-password"
SESSION_SECRET="change-me-to-random-32-chars-minimum"
OWNER_PASSWORD_HASH=""

# App
NODE_ENV="development"

# Production only (docker-compose.prod.yml)
# DB_PASSWORD="generate-with-openssl-rand-base64-24"
